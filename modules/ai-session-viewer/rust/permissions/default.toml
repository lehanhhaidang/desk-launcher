"$schema" = "schemas/schema.json"

[default]
description = "Allows all AI Session Viewer commands."
permissions = [
    "allow-list-providers",
    "allow-list-projects",
    "allow-list-sessions",
    "allow-read-session",
    "allow-delete-session",
    "allow-rename-session",
]
