{
  "name": "desk-launcher",
  "private": true,
  "version": "0.1.0",
  "description": "Unified desktop tool launcher hosting modular sub-apps",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "sidecars": "node scripts/ensure-sidecars.mjs",
    "dev": "npm run sidecars && npm --workspace @desk-launcher/launcher run tauri dev",
    "build": "npm run sidecars && npm --workspace @desk-launcher/launcher run tauri build",
    "frontend:dev": "npm --workspace @desk-launcher/launcher run dev",
    "frontend:build": "npm --workspace @desk-launcher/launcher run build",
    "tauri": "npm --workspace @desk-launcher/launcher run tauri"
  }
}
