{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher-default",
  "description": "Default permissions for the launcher window (dashboard). Module windows get their own capability files.",
  "windows": ["launcher"],
  "permissions": [
    "core:default",
    "opener:default",
    "log:default",
    "dialog:default"
  ]
}
